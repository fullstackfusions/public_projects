// Canonical location: src/features/scheduler/api.ts
// This re-export preserves backwards compatibility for existing consumers.
export { schedulerApi } from '../features/scheduler/api'
export type { Event, EventPayload, EventUpdatePayload, Reminder, ReminderPayload, ReminderUpdatePayload } from '../features/scheduler/types'
